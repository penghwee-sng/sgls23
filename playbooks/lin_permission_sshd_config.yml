- name: sshd config accessible by root only
  hosts: linux
  become: yes
  tasks:
  - name: Change file ownership, group and permissions
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      owner: root
      group: root
      mode: og-rwx